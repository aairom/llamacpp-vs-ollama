PORT=8088
DEBUG=false
# Maximum seconds to wait for a single benchmark run (increase for slow hardware or large models)
# gemma-3-270m on CPU needs ~2-3 min; raise this if you get timeout errors
BENCH_TIMEOUT=600
